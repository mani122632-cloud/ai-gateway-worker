{
  "name": "ai-gateway-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Cloudflare Worker acting as a secure AI Gateway between the CRM app and the Gemini API. Independent project — not part of the CRM codebase.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:gemini": "wrangler secret put GEMINI_API_KEY",
    "secret:app-token": "wrangler secret put APP_SHARED_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
